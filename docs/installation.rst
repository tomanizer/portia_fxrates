============
Installation
============

At the command line::

    $ easy_install portia_fxrates

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv portia_fxrates
    $ pip install portia_fxrates
